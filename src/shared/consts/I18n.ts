import { TLanguages } from '../types';

export const DEFAULT_LNG: TLanguages = 'es';
export const LANGUAGES: TLanguages[] = [DEFAULT_LNG, 'en'];
export const DEFAULT_FILE_NAME = 'typography';
export const I18N_COOKIE_NAME = 'i18next';
