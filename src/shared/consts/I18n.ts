import { ELocales } from '../types';

export const DEFAULT_LOCALE: ELocales = ELocales.EN;
export const LANGUAGES: ELocales[] = [DEFAULT_LOCALE];
export const DEFAULT_FILE_NAME = 'typography';
export const I18N_COOKIE_NAME = 'i18next';
